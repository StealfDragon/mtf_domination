// class Reticle extends Phaser.GameObjects.Sprite {
//     constructor(scene, x, y) {
//       // 'reticle' is the key for TempDominationReticle.png
//       super(scene, x, y, 'reticle');
//       scene.add.existing(this);
//     }
  
//     // Update method to change position based on input
//     update(cursors) {
//       // For example, using keyboard arrow keys
//       if (cursors.left.isDown) { this.x -= 5; }
//       if (cursors.right.isDown) { this.x += 5; }
//       if (cursors.up.isDown) { this.y -= 5; }
//       if (cursors.down.isDown) { this.y += 5; }
//     }
//   }

  